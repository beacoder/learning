
;; 在minibuffer里启用自动补全函数和变量
(icomplete-mode t)

;; 默认启用 Ido-mode 模式
(ido-mode t)

;; 默认启用 Narrow 模式
(put 'narrow-to-region 'disabled nil)

;; 自动成对插入括号
(electric-pair-mode t)

;; 保存桌面
(desktop-save-mode t)
