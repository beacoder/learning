# Linux Common Command

0. jobs                                   // list commands running in background
1. Ctrl + z                               // pause the process
2. fg [%jobspec]                          // bring to foregound
3. kill jobs                              // kill -signal %jobnumber
e.g:
kill -l
kill -15 %1
kill -9 %1
kill -SIGTERM %1
kill -SIGKILL %1

4. scp file brightc@atslxws114:/tmp/      // 不同的 workstation 之间拷贝文件
5. find -L(-H) directory -name filename   // 递归遍历指定目录(软链接、硬链接),查找文件名
6. grep -Rs "pattern" directory           // 递归遍历目录下所有文件和文件内容,查找对应的 pattern
7. ldd -r libxxx.so                       // 输出 libxxx.so 的依赖关系以及未定义的 symbol
8. nm -g libxxx.so                        // 输出 libxxx.so 包含的 symbol
9. gcc 设置链接库与运行库查找路径
      -L                                  // 设置链接库查找路径
      -rpath                              // 设置运行库查找路径
10. tar zcvf xxx.tar directory            // 将 directory 压缩为文件
    tar zxvf xxx.tar                      // 解压到当前目录 
11. pgrep -f proc-name                    // 获取进程 ID
12. ps uH p proc-id | wc -l               // 获取进程当前 active 的线程数
13. awk 'NR==2,NR==3' test.log            // 获取 test.log 第2、3行的内容
      awk '{print $(n)}'                  // 获取某一行的第 n 个字段的内容
14. pwdx "proc-id"                        // 获取进程当前工作路径
15. /usr/sbin/lsof -n | grep tcmalloc     // 显示当前使用了动态库(tcmalloc)的进程
16. objdump -S -d executable              // 显示可执行文件的汇编指令
17. gprof                                 // 监测程序中各函数执行的性能数据
18. /etc/sudoers                          // 修改此文件添加 sudo 权限
19. find ./ *.gz | xargs -i gzip -d -f {} // 解压当前目录所有压缩文件
20. tee                                   // 读取标准输入并将内容输出到标准输出和指定(一个或多个)文件中
21. which command                         // 显示 command 的完整路径
22. locate                                // 在 updatedb 所建立的数据库中查找 Linux 系统中的文件,速度比 find 快，但查找的数据可能不是最新的
23. set/unset [variable]                  // 查询、设置、取消环境变量
24. watch -d -n 5 command                 // 每隔5秒执行一次 command,并高亮显示变化的地方
25. egrep "(key1|key2).+key3" file        // 扩展正则表达式匹配搜索
26. du -sh *                              // 显示当前目录下，各个文件夹的占用空间
27. tail -f log                           // 实时刷新显示 log 的结尾内容
28. HereDoc                               // 在 Bash 或 Perl 中处理跨越多行的字符串很方便
29. strings binary                        // 用于输出binary文件中的text内容
30. grep 10197 mvea.log | sort | uniq     // 排序并去除重复记录
31. date +%w | tr -d '\n'                 // 删除换行符
