# Shell Scripts

# create tag file for emacs using ctags
ctags -e -R .

# create tag file for specific extentions
ctags -e -R *.cpp *.hpp *.h

# create tag file using etags
find . -type f -iname "*.[ch]" | etags -

# list commands running in background
jobs

# bring process back to background
Ctrl + z     => pause the process
bg [jobspec] => bring to background

# bring process forward to foreground
Ctrl + z      => pause the process
fg [jobspec]  => bring to foregound

# enlarge the shell history size
export HISTSIZE=100000
export HISTFILESIZE=100000

